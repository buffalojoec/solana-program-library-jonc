# TypeScript bindings for record program

For use with both node.js and in-browser.

## Installation

```
npm install
```

## Build and run

In the `client` folder:

```
npm run build
```

## Test

```
npm start
```